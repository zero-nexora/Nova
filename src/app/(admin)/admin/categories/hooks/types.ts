export interface uploadedImage {
  url: string;
  publicId: string;
}

export interface LocalImagePreview {
  id: string;
  base64Url: string;
}

export interface UploadedImage {
  publicId: string;
  imageUrl: string;
}